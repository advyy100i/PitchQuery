packages:
  # Only for its generic tests — accepted_range and unique_combination_of_columns
  # are the two assertions this warehouse needs and dbt core does not ship.
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
