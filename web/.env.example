# The deployed API. On Vercel set this in Project Settings > Environment
# Variables; locally the default in next.config.mjs (localhost:8000) applies.
NEXT_PUBLIC_API_URL=https://pitchquery-api.onrender.com
